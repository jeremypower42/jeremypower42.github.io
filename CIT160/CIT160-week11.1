<!DOCTYPE html>
<html lang="en">
  <head>
    <title>CIT160 Week 11</title>
    <meta charset="utf-8">
    
    <script>
      function doPayment (){
        
        var payment = computePayment (principal, annualRate, years, periodsPerYear);
        
        document.getElementById("payment").innerHTML = payment;
        
      }

      function doBalance (){
        
        var balance = computeBalance (principal, annualRate, years, periodsPerYear, numberOfPaymentPaidToDate);
        
        document.getElementById("balance").innerHTML = balance;
        
      }

      function computePayment (principal, annualRate, years, periodsPerYear){
        
        var a = pricipal;
        var r = annualRate/periodsPerYear;
        var n = periodsPerYear * years;;
        
        var p = (ar)/(1-(1+r)**-n);
                
      }

      function computeBalance (principal, annualRate, years, periodsPerYear, numberOfPaymentPaidToDate){
        
        var a = pricipal;
        var r = annualRate/periodsPerYear;
        var p = periodsPerYear;
        var d = numberOfPaymentPaidToDate;
        
        b = (a(1+r)**d) - ((p(1+r)**d - 1)/ r)
        
      }
        
  </script>
     
  </head>  
  
  <body>
  
    Amount borrowed(principal)<input type="text" id="principal"><br><br>
  
    Annual rate(Example: .065)<input type = "text" id = "annualRate"><br><br>
    
    Number of years<input type = "text" id = "years"><br><br>
    
    Payment per year<input type = "text" id = "periodsPerYear"><br><br>
  
    <button class="button" onclick="doPayment()">Compute payment</button><br><br>
    
    <div id="payment"></div>
    
    Number of payments paid to date<input type="text" id="numberOfPaymentPaidToDate"><br><br>
  
    <button class="button" onclick="doBalance">Compute payoff amount</button>

     <div id="balance"></div>
    
  </body>
</html>
